stages:
  - test

smoke-tests:
  stage: test
  image: node:18
  before_script:
    - cd frontend
    - npm ci
  script:
    - npm run test:smoke
  variables:
    BACKEND_BASE_URL: ${BACKEND_URL:-"http://localhost/Jacom-Platform/backend"}
  artifacts:
    when: always
    paths:
      - frontend/test-results.json
    expire_in: 30 days
  only:
    - main
    - develop
    - merge_requests
